# Campaign List Project

App created with React to handle creation of items through a modal.

## Installation

- Clone Repository.
- Run `npm install` to install dependencies.
- Run `npm start` to run the project.
